---
import Field from "./Field.astro";

interface Props {
  id: string;
  label: string;
  name?: string;
  type?: string;
  size?: "sm" | "md" | "lg";
  hint?: string;
  error?: string;
  required?: boolean;
  disabled?: boolean;
  readonly?: boolean;
  hideLabel?: boolean;
  placeholder?: string;
  value?: string;
  class?: string;
}

const {
  id, label, name = id, type = "text", size = "md", hint, error,
  required = false, disabled = false, readonly = false, hideLabel = false,
  placeholder, value, class: className, ...rest
} = Astro.props;

const describedBy = [hint && `${id}-hint`, error && `${id}-error`]
  .filter(Boolean)
  .join(" ") || undefined;
---
<Field id={id} label={label} hint={hint} error={error} required={required} hideLabel={hideLabel} class={className}>
  <input
    id={id}
    name={name}
    type={type}
    class:list={["ink-input", size !== "md" && `ink-input--${size}`]}
    value={value}
    placeholder={placeholder}
    required={required || undefined}
    disabled={disabled || undefined}
    readonly={readonly || undefined}
    aria-invalid={error ? "true" : undefined}
    aria-describedby={describedBy}
    {...rest}
  />
</Field>
