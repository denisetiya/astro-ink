---
interface Props {
  variant?: "neutral" | "accent" | "success" | "warning" | "danger" | "info";
  style?: "solid" | "outline" | "soft";
  size?: "sm" | "md";
  class?: string;
}

const { variant = "neutral", style = "solid", size = "md", class: className, ...rest } = Astro.props;
---
<span class:list={["ink-badge", `ink-badge--${variant}`, `ink-badge--${size}`, `ink-badge--${style}`, className]} {...rest}>
  <slot />
</span>

<style>
  .ink-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--ink-space-1);
    font-weight: var(--ink-font-weight-medium);
    line-height: 1;
    border-radius: var(--ink-badge-radius, var(--ink-radius-sm));
    white-space: nowrap;
    border: 1px solid transparent;
  }

  .ink-badge--sm {
    padding: 0.125rem var(--ink-space-2);
    font-size: var(--ink-text-xs);
  }

  .ink-badge--md {
    padding: var(--ink-space-1) var(--ink-space-2);
    font-size: var(--ink-text-sm);
  }

  .ink-badge--neutral {
    background: color-mix(in srgb, var(--ink-ink) 8%, transparent);
    color: var(--ink-ink);
  }

  .ink-badge--accent {
    background: var(--ink-accent);
    color: var(--ink-accent-ink);
  }

  .ink-badge--success {
    background: color-mix(in srgb, var(--ink-success) 14%, transparent);
    color: var(--ink-success-text, var(--ink-success));
  }

  .ink-badge--warning {
    background: color-mix(in srgb, var(--ink-warning) 14%, transparent);
    color: var(--ink-warning-text, var(--ink-warning));
  }

  .ink-badge--danger {
    background: color-mix(in srgb, var(--ink-danger) 14%, transparent);
    color: var(--ink-danger-text, var(--ink-danger));
  }

  .ink-badge--info {
    background: color-mix(in srgb, var(--ink-info) 14%, transparent);
    color: var(--ink-info-text, var(--ink-info));
  }

  .ink-badge--outline { background: transparent; }
  .ink-badge--outline.ink-badge--neutral { border-color: var(--ink-border-strong); }
  .ink-badge--outline.ink-badge--accent { border-color: var(--ink-accent); color: var(--ink-accent); }
  .ink-badge--outline.ink-badge--success { border-color: var(--ink-success); color: var(--ink-success); }
  .ink-badge--outline.ink-badge--warning { border-color: var(--ink-warning); color: var(--ink-warning); }
  .ink-badge--outline.ink-badge--danger { border-color: var(--ink-danger); color: var(--ink-danger); }
  .ink-badge--outline.ink-badge--info { border-color: var(--ink-info); color: var(--ink-info); }
</style>
