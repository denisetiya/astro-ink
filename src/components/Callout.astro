---
interface Props {
  variant?: "info" | "success" | "warning" | "danger";
  style?: "tint" | "filled";
  title?: string;
  class?: string;
}

const { variant = "info", style = "tint", title, class: className, ...rest } = Astro.props;
---
<section
  class:list={["ink-callout", `ink-callout--${variant}`, `ink-callout--${style}`, className]}
  aria-label={title}
  {...rest}
>
  {title && <p class="ink-callout__title">{title}</p>}
  <div class="ink-callout__body"><slot /></div>
</section>

<style>
  .ink-callout {
    border-inline-start: 3px solid var(--ink-border-strong);
    padding: var(--ink-space-3) var(--ink-space-4);
    background: color-mix(in srgb, var(--ink-ink) 3%, var(--ink-surface));
    border-radius: 0 var(--ink-radius-md) var(--ink-radius-md) 0;
    font-size: var(--ink-text-sm);
  }

  .ink-callout__title {
    font-weight: var(--ink-font-weight-semibold);
    margin-bottom: var(--ink-space-1);
  }

  .ink-callout--info { border-inline-start-color: var(--ink-info); }
  .ink-callout--success { border-inline-start-color: var(--ink-success); }
  .ink-callout--warning { border-inline-start-color: var(--ink-warning); }
  .ink-callout--danger { border-inline-start-color: var(--ink-danger); }

  .ink-callout--filled.ink-callout--info { background: var(--ink-info); border-inline-start-color: var(--ink-info); }
  .ink-callout--filled.ink-callout--success { background: var(--ink-success); border-inline-start-color: var(--ink-success); }
  .ink-callout--filled.ink-callout--warning { background: var(--ink-warning); border-inline-start-color: var(--ink-warning); }
  .ink-callout--filled.ink-callout--danger { background: var(--ink-danger); border-inline-start-color: var(--ink-danger); }
  .ink-callout--filled, .ink-callout--filled .ink-callout__title, .ink-callout--filled .ink-callout__body { color: #ffffff; }
</style>
